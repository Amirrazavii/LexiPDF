// import
// static dependencies
// async loading
// live bindings



// پس import ها static هستند.

// میشه گفت  انگار هر چه   export می کنیم مثل یک pointer به اون مقدار هست 



// ESM live bindingها را پیاده‌سازی می‌کند.
// این bindingها read-only هستند (نمی‌توانید مقدار binding را عوض کنید).
// اگر binding به یک object اشاره کند، properties آن object قابل تغییر هستند (مگر اینکه object با Object.freeze محافظت شده باشد).